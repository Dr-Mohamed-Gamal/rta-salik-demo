[project]
name = "salik"
version = "0.1.0"
description = "Emulator of the RTA Dubai Salik electronic toll system."
requires-python = ">=3.10"

[project.scripts]
salik-demo = "salik.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
